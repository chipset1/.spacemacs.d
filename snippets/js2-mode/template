# -*- mode: snippet -*-
# name: template
# key: template
# --
import p5 from "p5";

function setup(){
    createCanvas(800, 800);
}

function draw(){
    background(0);
    $0
}

window.setup = setup;
window.draw = draw;